## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.\n\n
[Zig](../../../index.md) &rsaquo; [std/](../../../std/index.md) &rsaquo; [mem/](../../../std/mem/index.md) &rsaquo; [Allocator.zig](../../../std/mem/Allocator.zig/index.md) &rsaquo; 
resize



# resize

Request to modify the size of an allocation.

It is guaranteed to not move the pointer, however the allocator
implementation may refuse the resize request by returning `false`.

`allocation` may be an empty slice, in which case `false` is returned,
unless `new_len` is also 0, in which case `true` is returned.

`new_len` may be zero, in which case the allocation is freed.
Allocator.resize

`pub fn resize(self: Allocator, allocation: anytype, new_len: usize) bool`

> [lib/std/mem/](../../../std/mem/index.md)Allocator.zig:317


