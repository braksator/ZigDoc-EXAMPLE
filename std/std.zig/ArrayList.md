## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../index.md) &rsaquo; [std/](../../std/index.md) &rsaquo; [std.zig](../../std/std.zig/index.md) &rsaquo; ArrayList**

# ArrayList

A contiguous, growable list of items in memory. This is a wrapper around a
slice of `T` values.

The same allocator must be used throughout its entire lifetime. Initialize
directly with `empty` or `initCapacity`, and deinitialize with `deinit` or
`toOwnedSlice`.

std.ArrayList

`pub fn ArrayList(comptime T: type) type`

> [lib/std/](../../std/index.md)std.zig:52


