## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../../index.md) &rsaquo; [std/](../../../../std/index.md) &rsaquo; [hash/](../../../../std/hash/index.md) &rsaquo; [crc/](../../../../std/hash/crc/index.md) &rsaquo; [Crc32c.zig](../../../../std/hash/crc/Crc32c.zig/index.md) &rsaquo; times**

# times

Computes `mat * vec` over `GF(2)`, where `mat` is a 32x32 binary matrix and `vec`
is a 32-bit vector. This somewhat "simulates" how bits propagate through the CRC register
during shifting.

- In GF(2) (aka a field where the only values are 0 and 1, aka binary), multiplication is
an `AND`, and addition is `XOR`.
- This dot product determines how each bit in the input vector "contributes" to
the final CRC state, by XORing (adding) rows of the matrix where `vec` has 1s.

Crc32c.times

`fn times(mat: *const [32]u32, vec: u32) u32`

> [lib/std/hash/crc/](../../../../std/hash/crc/index.md)Crc32c.zig:114


