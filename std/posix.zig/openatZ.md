## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../index.md) &rsaquo; [std/](../../std/index.md) &rsaquo; [posix.zig](../../std/posix.zig/index.md) &rsaquo; openatZ**

# openatZ

Open and possibly create a file. Keeps trying if it gets interrupted.
`file_path` is relative to the open directory handle `dir_fd`.
On Windows, `file_path` should be encoded as [WTF-8](https://wtf-8.codeberg.page/).
On WASI, `file_path` should be encoded as valid UTF-8.
On other platforms, `file_path` is an opaque sequence of bytes with no particular encoding.
See also `openat`.

posix.openatZ

`pub fn openatZ(dir_fd: fd_t, file_path: [*:0]const u8, flags: O, mode: mode_t) OpenError!fd_t`

> [lib/std/](../../std/index.md)posix.zig:457


