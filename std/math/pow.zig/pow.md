## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.

**
[Zig](../../../index.md) &rsaquo; [std/](../../../std/index.md) &rsaquo; [math/](../../../std/math/index.md) &rsaquo; [pow.zig](../../../std/math/pow.zig/index.md) &rsaquo; 
pow


**
# pow

Returns x raised to the power of y (x^y).

Special Cases:
 - pow(x, +-0)    = 1 for any x
 - pow(1, y)      = 1 for any y
 - pow(x, 1)      = x for any x
 - pow(nan, y)    = nan for any y != 0
 - pow(x, nan)    = nan for any x != 1
 - pow(+-0, y)    = +-inf for y an odd integer < 0
 - pow(+-0, -inf) = +inf
 - pow(+-0, +inf) = +0
 - pow(+-0, y)    = +inf for finite y < 0 and not an odd integer
 - pow(+-0, y)    = +-0 for finite y an odd integer > 0
 - pow(+-0, y)    = +0 for finite y > 0 and not an odd integer
 - pow(-1, +-inf) = 1
 - pow(x, +inf)   = +inf for |x| > 1
 - pow(x, -inf)   = +0 for |x| > 1
 - pow(x, +inf)   = +0 for |x| < 1
 - pow(x, -inf)   = +inf for |x| < 1
 - pow(+inf, y)   = +inf for y > 0
 - pow(+inf, y)   = +0 for y < 0
 - pow(-inf, y)   = pow(-0, -y)
 - pow(x, y)      = nan for finite x < 0 and finite non-integer y

pow.pow

`pub fn pow(comptime T: type, x: T, y: T) T`

> [lib/std/math/](../../../std/math/index.md)pow.zig:33


