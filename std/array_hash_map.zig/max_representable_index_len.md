## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.

**
[Zig](../../index.md) &rsaquo; [std/](../../std/index.md) &rsaquo; [array_hash_map.zig](../../std/array_hash_map.zig/index.md) &rsaquo; 
max_representable_index_len


**
# max_representable_index_len

the byte size of the index must fit in a usize.  This is a power of two
length * the size of an Index(u32).  The index is 8 bytes (3 bits repr)
and max_usize + 1 is not representable, so we need to subtract out 4 bits.

array_hash_map.max_representable_index_len

`const max_representable_index_len = @bitSizeOf(usize) - 4`

> [lib/std/](../../std/index.md)array_hash_map.zig:1620


