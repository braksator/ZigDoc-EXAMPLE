## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../index.md) &rsaquo; [std/](../../std/index.md) &rsaquo; [array_list.zig](../../std/array_list.zig/index.md) &rsaquo; 
Aligned
**

# Aligned

A contiguous, growable list of arbitrarily aligned items in memory.
This is a wrapper around an array of T values aligned to `alignment`-byte
addresses. If the specified alignment is `null`, then `@alignOf(T)` is used.

Functions that potentially allocate memory accept an `Allocator` parameter.
Initialize directly or with `initCapacity`, and deinitialize with `deinit`
or use `toOwnedSlice`.

Default initialization of this struct is deprecated; use `.empty` instead.

array_list.Aligned

`pub fn Aligned(comptime T: type, comptime alignment: ?mem.Alignment) type`

> [lib/std/](../../std/index.md)array_list.zig:567


