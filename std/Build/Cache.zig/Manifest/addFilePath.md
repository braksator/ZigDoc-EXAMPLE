## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.

[Zig](../../../../index.md) &rsaquo; [std/](../../../../std/index.md) &rsaquo; [Build/](../../../../std/Build/index.md) &rsaquo; [Cache.zig](../../../../std/Build/Cache.zig/index.md) &rsaquo; [Manifest](../../../../std/Build/Cache.zig/Manifest/index.md) &rsaquo; 
addFilePath



# addFilePath

Add a file as a dependency of process being cached. When `hit` is
called, the file's contents will be checked to ensure that it matches
the contents from previous times.

Max file size will be used to determine the amount of space the file contents
are allowed to take up in memory. If max_file_size is null, then the contents
will not be loaded into memory.

Returns the index of the entry in the `files` array list. You can use it
to access the contents of the file after calling `hit()` like so:

```
var file_contents = cache_hash.files.keys()[file_index].contents.?;
```
Cache.Manifest.addFilePath

`pub fn addFilePath(m: *Manifest, file_path: Path, max_file_size: ?usize) !usize`

> [lib/std/Build/](../../../../std/Build/index.md)Cache.zig:403


