## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../../index.md) &rsaquo; [std/](../../../../std/index.md) &rsaquo; [Build/](../../../../std/Build/index.md) &rsaquo; [Step/](../../../../std/Build/Step/index.md) &rsaquo; [Run.zig](../../../../std/Build/Step/Run.zig/index.md) &rsaquo; addOutputDirectoryArg2**

# addOutputDirectoryArg2

Provides a directory path as a command line argument to the command being run.
Asserts `basename` is not empty.

For example, a prefix of "-o" and basename of "output_dir" will result in
the child process seeing something like this: "-ozig-cache/.../output_dir"

The child process will see a single argument, regardless of whether the
prefix or basename have spaces.

The returned `std.Build.LazyPath` can be used as inputs to other APIs
throughout the build system.

Related:
* `addDirectoryArg` - for input directories given to the child process

Run.addOutputDirectoryArg2

`pub fn addOutputDirectoryArg2(
    run: *Run,
    basename: []const u8,
    options: PathArgOptions,
) std.Build.LazyPath`

> [lib/std/Build/Step/](../../../../std/Build/Step/index.md)Run.zig:445


