## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../index.md) &rsaquo; [std/](../../../std/index.md) &rsaquo; [Io/](../../../std/Io/index.md) &rsaquo; [Dir.zig](../../../std/Io/Dir.zig/index.md) &rsaquo; walkSelectively**

# walkSelectively

Recursively iterates over a directory, but requires the user to
opt-in to recursing into each directory entry.

`dir` must have been opened with `OpenOptions.iterate` set to `true`.

`Walker.deinit` releases allocated memory and directory handles.

The order of returned file system entries is undefined.

`dir` will not be closed after walking it.

See also `walk`.

Dir.walkSelectively

`pub fn walkSelectively(dir: Dir, allocator: Allocator) !SelectiveWalker`

> [lib/std/Io/](../../../std/Io/index.md)Dir.zig:327


