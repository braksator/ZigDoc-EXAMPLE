## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../../index.md) &rsaquo; [std/](../../../../std/index.md) &rsaquo; [debug/](../../../../std/debug/index.md) &rsaquo; [Dwarf/](../../../../std/debug/Dwarf/index.md) &rsaquo; [Unwind.zig](../../../../std/debug/Dwarf/Unwind.zig/index.md) &rsaquo; 
prepare
**

# prepare

Builds the CIE list and FDE lookup table if they are not already built. It is required to call
this function at least once before calling `lookupPc` or `getFde`. If only `getFde` is needed,
then `need_lookup` can be set to `false` to make this function more efficient.

Unwind.prepare

`pub fn prepare(
    unwind: *Unwind,
    gpa: Allocator,
    addr_size_bytes: u8,
    endian: Endian,
    need_lookup: bool,
    /// The `__eh_frame` section in Mach-O binaries deviates from the standard `.eh_frame` section
    /// in one way which this function needs to be aware of.
    is_macho: bool,
) !void`

> [lib/std/debug/Dwarf/](../../../../std/debug/Dwarf/index.md)Unwind.zig:472


