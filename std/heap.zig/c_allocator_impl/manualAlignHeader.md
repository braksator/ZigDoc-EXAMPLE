## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by <b><a href=http://github.com/braksator/ZigDoc target=_blank>ZigDoc</a></b>, an alternative to Zig's built-in <em>Auto Doc</em> feature. See also <a href=/zigdoc>examples in other modes/formats</a>. The project being documented here (as the example) is the Zig library itself.
[Zig](../../../index.md) &rsaquo; [std/](../../../std/index.md) &rsaquo; [heap.zig](../../../std/heap.zig/index.md) &rsaquo; [c_allocator_impl](../../../std/heap.zig/c_allocator_impl/index.md) &rsaquo; 
manualAlignHeader



# manualAlignHeader

If `allocStrat(a) == .manual_align`, an allocation looks like this:

unaligned_ptr   hdr_ptr  aligned_ptr
v               v        v
+---------------+--------+--------------+
|    padding    | header | usable bytes |
+---------------+--------+--------------+

* `unaligned_ptr` is the raw return value of `malloc`.
* `aligned_ptr` is computed by aligning `unaligned_ptr` forward; it is what `alloc` returns.
* `hdr_ptr` points to a pointer-sized header directly before the usable space. This header
  contains the value `unaligned_ptr`, so that we can pass it to `free` later. This is
  necessary because the width of the padding is unknown.

This function accepts `aligned_ptr` and offsets it backwards to return `hdr_ptr`.
heap.c_allocator_impl.manualAlignHeader

`fn manualAlignHeader(aligned_ptr: [*]u8) *[*]u8`

> [lib/std/](../../../std/index.md)heap.zig:202


