## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../index.md) &rsaquo; [std/](../../../std/index.md) &rsaquo; [heap/](../../../std/heap/index.md) &rsaquo; [PageAllocator.zig](../../../std/heap/PageAllocator.zig/index.md) &rsaquo; stack_direction**

# stack_direction

On operating systems that don't immediately map in the whole stack, we need
to be careful to not hint into the pages after the stack guard gap, which
the stack will expand into. The easiest way to avoid that is to hint in the
same direction as stack growth.

PageAllocator.stack_direction

`const stack_direction = builtin.target.stackGrowth()`

> [lib/std/heap/](../../../std/heap/index.md)PageAllocator.zig:35


