## Zig 0.17.0-dev (Markdown example)
This is an example of documentation generated by **[ZigDoc](http://github.com/braksator/ZigDoc)**, an alternative to Zig's built-in *Auto Doc* feature. See also [examples in other modes/formats](https://braksator.github.io/zigdoc/). The project being documented here (as the example) is the Zig library itself.



**[Zig](../../../../index.md) &rsaquo; [std/](../../../../std/index.md) &rsaquo; [heap/](../../../../std/heap/index.md) &rsaquo; [SafeAllocator.zig](../../../../std/heap/SafeAllocator.zig/index.md) &rsaquo; [AllocFooter](../../../../std/heap/SafeAllocator.zig/AllocFooter/index.md) &rsaquo; 
populate
**

# populate

It is the caller's responsibility to `.acquire` fence the respective `Bucket.alloc_count`.

SafeAllocator.AllocFooter.populate

`fn populate(
        memory: []align(@alignOf(AllocFooter)) u8,
        len: usize,
        alignment: Alignment,
        ra: usize,
        /// `true` for large allocations
        is_extended: bool,
        /// `false` for large allocations
        prev_extended: bool,
        container: Extended.Container,
        s: *SafeAllocator,
    ) *AllocFooter`

> [lib/std/heap/](../../../../std/heap/index.md)SafeAllocator.zig:579


